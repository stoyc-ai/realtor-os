---
description: Set up your new 24/7 assistant — quick, tap-through onboarding (areas, price band, niche, voice, rules, and writing samples)
---

The agent has run `/onboard`. Set them up by running the **onboarding-agent** skill in
**button mode**: use the `AskUserQuestion` tool to present the setup **choice** questions as
clickable options (not a wall of text). Note: free-text answers (exact towns/ZIPs, sign-off,
writing samples, the "anything else" catch-all) are asked as a plain chat message, never as a
button card — the skill spells out exactly which is which. Follow it precisely.

Frame the whole thing as briefing a brand-new 24/7 executive assistant. Open warmly, then
walk the screens in order, show the running checklist after each step, capture writing
samples and the free-text catch-all, and finish on the "you're all set" summary.

Invoke the onboarding-agent skill now and follow it precisely.
